# Web framework
fastapi==0.115.0
uvicorn[standard]==0.49.0
python-multipart==0.0.32
sse-starlette==2.1.3

# Config
pydantic==2.13.4
pydantic-settings==2.5.2
python-dotenv==1.0.1

# Agent + LLM
langgraph==0.2.39
langchain-core==0.3.86
langchain-openai==0.2.14          # ChatOpenAI.bind_tools over Groq (tool-worker)
langchain-mcp-adapters==0.1.7     # plug-and-play MCP tools (MultiServerMCPClient)
mcp==1.27.2                       # MCP client + FastMCP server
openai==1.109.1                   # OpenAI-compatible client (works with Groq base_url)
langsmith==0.8.9                  # observability (LangSmith — your Etech7 stack)

# Embeddings (local, no API key, open-source ONNX)
fastembed==0.4.1

# Vector + metadata DB (Supabase Postgres + pgvector)
psycopg[binary]==3.2.3
pgvector==0.3.6

# Web search tool
tavily-python==0.5.0

# Text extraction
pymupdf==1.24.10
python-docx==1.2.0

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
